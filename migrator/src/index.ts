/**
 * SysCreditos Migrator: Firestore -> MongoDB.
 * Fase 2 del roadmap. Se implementa cuando confirmemos el string de conexion Mongo.
 * Uso: npm run migrate:dry | npm run migrate:run  (desde la raiz del workspace)
 */
console.log('[migrator] pendiente de implementacion (Fase 2 del roadmap).');
